# Contributing

Keep changes focused and backwards-compatible where practical.

Requirements:

- no hardcoded provider or model IDs;
- no weakening of role boundaries without an explicit rationale;
- no automatic `git push`;
- no destructive defaults;
- no credentials in fixtures, tests, docs, or examples;
- keep templates and scripts small and maintainable;
- update verification tests when configuration behavior changes.
