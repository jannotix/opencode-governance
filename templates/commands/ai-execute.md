---
description: Execute an explicit approved governance plan
agent: executor
subtask: true
---

Execute the approved plan identified by:

$ARGUMENTS

Locate the explicit plan in `.ai/tasks/`. If no single unambiguous approved plan can be identified, return `BLOCKED` instead of guessing.

Implement, test and write the execution report. Do not push changes.
