# Security

Do not report credentials, tokens, private keys, or other secrets in public issues.

Before opening a security report, remove all sensitive values and replace them with clearly marked redactions.

The workflow is designed to prevent source edits by Architect and Reviewer and to deny automatic `git push`. Users should still review the generated configuration after installation and before using it on production repositories.
